/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx } from "@emotion/react";

const Template = () => {
  return <div css={styles.body}>Template</div>;
};

const styles = {
  body: {
    backgroundColor: "white",
  },
};

export default Template;
