schema: http://localhost:4000
generates:
  ../functions/src/@customTypes/graphql-types.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
  ./build/graphql-types.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true